# Production environment

Production is a separately approved root. Copy the reviewed `dev` root, use a distinct remote-state key and CIDR values, set `environment = "prod"`, and require GitHub Environment approval. There is no automated apply or destroy.
