# Contributing

Create feature branches as `feature/<component>`. Run formatting, validation, and policy tests before opening a pull request. Do not include state, plans, credentials, generated provider lock changes unrelated to your work, or destructive workflow changes. Infrastructure pull requests must include a reviewed plan artifact and cost impact.
